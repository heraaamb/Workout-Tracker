
  # Workout Tracker Mobile UI

  This is a code bundle for Workout Tracker Mobile UI. The original project is available at https://www.figma.com/design/yZxWMoY9KJ7ZHZIhWuPpLu/Workout-Tracker-Mobile-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  